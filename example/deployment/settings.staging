#!/bin/bash

# Settings specific to the 'staging' deployment target.
# USER is the ssh user, SERVER is the ssh host. USER should
# match the USER setting in /opt/stagecoach/settings on
# the server

USER=staging
SERVER=staging.punkave.net
